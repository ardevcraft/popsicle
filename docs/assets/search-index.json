[{"title":"Overview","description":"Popsicle 2.1 overview: small API, explicit state, UI = f(state).","url":"index.html","text":"Popsicle Flutter state management dependency injection Store IntentStore commit effect ui History streams AsyncState Scope get use","keywords":"Popsicle create params value inject Store IntentStore History Streams"},{"title":"Getting Started","description":"Start using Popsicle with the four declaration APIs and UI projection.","url":"getting-started.html","text":"Getting Started Wrap the app: The four recommended declarations are: Example: Store: Render small state: Render Store state:","keywords":"dart\nimport 'package:popsicle/popsicle.dart';\n \n\nWrap the app:\n\n dart\nrunApp(\n  const Popsicle(\n    child: MyApp(),\n  ),\n);\n \n\nThe four recommended declarations are:\n\n dart\nPopsicle.inject(...)\nPopsicle.value(...)\nPopsicle.create(...)\nPopsicle.params(...)\n \n\nExample:\n\n dart\nfinal api = Popsicle.inject((_) => ApiClient());\nfinal count = Popsicle.value(0);\nfinal counter = Popsicle.create((_) => CounterStore());\n \n\nStore:\n\n dart\nclass CounterStore extends Store<int> {\n  CounterStore() : super(0);\n\n  void increment() => commit(state + 1);\n}\n \n\nRender small state:\n\n dart\ncount.ui((value) => Text('$value'));\n \n\nRender Store state:\n\n dart\ncounter.ui(\n  (context, state, store) => Text('$state'),\n);\n"},{"title":"Core Concepts","description":"Understand Popsicle state, effects, Scope, Stores, and the UI = f(state) model.","url":"core-concepts.html","text":"Core Concepts State and UI Popsicle separates persistent state from one shot effects: External streams feed the same state mechanism: History is optional: Declarations Scope Structured state Use Store<State by default. Use IntentStore<State, Intent when an explicit intent boundary improves the workflow. State used with History<State should be immutable.","keywords":"text\nUI = f(state)\n \n\nPopsicle separates persistent state from one-shot effects:\n\n text\nStore\n├── commit(state) -> persistent UI state\n└── effect(value) -> one-shot UI work\n \n\nExternal streams feed the same state mechanism:\n\n text\nStream -> listenTo -> Store -> commit -> UI\n \n\nHistory is optional:\n\n text\nStore + History<State> -> undo / redo\n \n\n## Declarations\n\n text\nPopsicle.inject -> dependency\nPopsicle.value  -> tiny reactive value\nPopsicle.create -> Store\nPopsicle.params -> parameterized Store\n \n\n## Scope\n\n text\nscope.get    -> access only\nscope.use    -> access + reactive dependency\nscope.store  -> Store instance\nscope.select -> reactive state projection\n \n\n## Structured state\n\nUse   by default. Use   when an explicit intent boundary improves the workflow.\n\nState used with "},{"title":"Dependency Injection","description":"Compose typed dependencies with Popsicle.inject, scope.get, and scope.use.","url":"dependency-injection.html","text":"Dependency Injection Use scope.get for ordinary constructor dependencies. Use scope.use when the dependency itself should recompute when another reactive declaration changes: Avoid service locator lookups inside Store methods. Resolve dependencies when the Store is created and keep constructor injection explicit.","keywords":"dart\nfinal apiClient = Popsicle.inject(\n  (_) => ApiClient(),\n);\n\nfinal repository = Popsicle.inject(\n  (scope) => UserRepository(\n    scope.get(apiClient),\n  ),\n);\n \n\nUse   for ordinary constructor dependencies.\n\nUse   when the dependency itself should recompute when another reactive declaration changes:\n\n dart\nfinal environment = Popsicle.value(Environment.production);\n\nfinal baseUrl = Popsicle.inject(\n  (scope) => scope.use(environment).baseUrl,\n);\n"},{"title":"Reactive Values","description":"Use Popsicle.value for small scoped reactive state.","url":"reactive-values.html","text":"Reactive Values Use Popsicle.value for a small standalone mutable value: Render: Mutate from a Scope: Use a Store instead when the state grows behavior, multiple related fields, async orchestration, or effects.","keywords":"Popsicle.value dart\nfinal selectedTab = Popsicle.value(0);\n \n\nRender:\n\n dart\nselectedTab.ui(\n  (index) => Text('$index'),\n);\n \n\nMutate from a Scope:\n\n dart\nscope.set(selectedTab, 2);\nscope.update(selectedTab, (index) => index + 1);\n"},{"title":"Stores & Effects","description":"Model persistent feature state with Store, commit, effects, and .ui().","url":"stores-effects.html","text":"Stores and Effects A Store owns persistent state and behavior: Declare: Render: commit Use commit(...) for persistent state transitions: effect Use effect(...) for one shot occurrences: Effects are not state, are not replayed on rebuild, do not rebuild Store UI by themselves, and are not included in History.","keywords":"dart\nclass ProfileStore extends Store<ProfileState> {\n  ProfileStore(this.repository) : super(const ProfileState());\n\n  final ProfileRepository repository;\n\n  Future<void> refresh() async {\n    final nextState = await repository.load();\n    commit(nextState);\n  }\n}\n \n\nDeclare:\n\n dart\nfinal profile = Popsicle.create(\n  (scope) => ProfileStore(\n    scope.get(profileRepository),\n  ),\n);\n \n\nRender:\n\n dart\nprofile.ui(\n  (context, state, store) {\n    return ProfileBody(\n      state: state,\n      onRefresh: store.refresh,\n    );\n  },\n  effect: (context, effect) {\n    // snackbar/navigation/dialog/etc.\n  },\n);\n \n\n##  \n\nUse   for persistent state transitions:\n\n dart\ncommit(nextState);\n \n\n##  \n\nUse   for one-shot occurrences:\n\n dart\neffect(const ProfileSaved());\n"},{"title":"Flutter UI","description":"Render reactive state with .ui(), PopsicleWidget, PopsicleBuilder, and PopsicleConsumer.","url":"flutter-ui.html","text":"Flutter UI Popsicle keeps Flutter rendering as a projection of state: Store .ui() Use .ui() as the normal Store rendering API: Attach one shot effects when the feature needs navigation, snackbars, dialogs, or similar UI work: ReactiveValue .ui() Small state projects directly to a widget: PopsicleWidget Use PopsicleWidget when one widget needs several declarations or direct Store access: The access rules stay explicit: PopsicleBuilder Use a builder when introducing a small reactive boundary inside an ordinary widget tree: Explicit PopsicleConsumer .ui() is implemented on top of PopsicleConsumer. Use the explicit widget when a named widget boundary is clearer: Prefer .ui() for ordinary feature rendering and keep PopsicleConsumer for explicit or reusable boundaries.","keywords":"text\nUI = f(state)\n \n\n## Store  \n\nUse   as the normal Store rendering API:\n\n dart\ncounter.ui(\n  (context, state, store) {\n    return FilledButton(\n      onPressed: store.increment,\n      child: Text('$state'),\n    );\n  },\n);\n \n\nAttach one-shot effects when the feature needs navigation, snackbars, dialogs, or similar UI work:\n\n dart\nprofile.ui(\n  (context, state, store) => ProfileBody(state: state),\n  effect: (context, effect) {\n    // Handle an occurrence once.\n  },\n);\n \n\n## ReactiveValue  \n\nSmall state projects directly to a widget:\n\n dart\nfinal selectedTab = Popsicle.value(0);\n\nselectedTab.ui(\n  (index) => Text('Tab $index'),\n);\n \n\n##  \n\nUse   when one widget needs several declarations or direct Store access:\n\n dart\nclass CounterActions extends PopsicleWidget {\n  const CounterActions({super.key});\n\n  @override\n  Widget build(BuildContext context, Scope scope) {\n    final count = scope.use(counter);\n    final store = scope.store(counter);\n\n    return Row(\n      children: [\n        Text('$count'),\n        IconButton(\n          onPressed: store.increment,\n          icon: const Icon(Icons.add),\n        ),\n      ],\n    );\n  }\n}\n \n\nThe access rules stay explicit:\n\n text\nscope.get(source)    -> current value, non-reactive\nscope.use(source)    -> current value + rebuild dependency\nscope.store(source)  -> Store instance, non-reactive\nscope.select(source, selector) -> rebuild only for the selected projection\n \n\n##  \n\nUse a builder when introducing a small reactive boundary inside an ordinary widget tree:\n\n dart\nPopsicleBuilder(\n  builder: (context, scope, child) {\n    final mode = scope.use(themeMode);\n    return Text('$mode');\n  },\n);\n \n\n## Explicit  \n\n  is implemented on top of  . Use the explicit widget when a named widget boundary is clearer:\n\n dart\nPopsicleConsumer<CounterStore, int>(\n  source: counter,\n  build: (context, state, store) {\n    return Text('$state');\n  },\n  effect: (context, effect) {\n    // Handle one-shot effect.\n  },\n);\n \n\nPrefer   for ordinary feature rendering and keep "},{"title":"IntentStore","description":"Use an explicit intent boundary for event-driven workflows.","url":"intent-store.html","text":"IntentStore IntentStore is optional structure for workflows where an explicit intent boundary improves readability. Dispatch with: Do not introduce intents for every button by default. Normal Store methods remain the simpler default.","keywords":"IntentStore text\nIntent -> IntentStore -> commit(State) -> UI\n                     \n\n dart\nsealed class LoginIntent {\n  const LoginIntent();\n}\n\nfinal class SubmitLogin extends LoginIntent {\n  const SubmitLogin();\n}\n\nclass LoginStore extends IntentStore<LoginState, LoginIntent> {\n  LoginStore() : super(const LoginState());\n\n  @override\n  Future<void> onIntent(LoginIntent intent) async {\n    switch (intent) {\n      case SubmitLogin():\n        // commit(nextState);\n        break;\n    }\n  }\n}\n \n\nDispatch with:\n\n dart\nstore.dispatch(const SubmitLogin());\n"},{"title":"History","description":"Add opt-in undo and redo to any immutable Store state.","url":"history.html","text":"History History<State adds opt in undo/redo to a Store. API: undo() and redo() return bool indicating whether a snapshot was restored. A normal commit(...) records the previous State and clears redo history. Undo/redo restore snapshots without recording themselves as new transitions. Effects are never recorded or replayed. Use immutable State with History so older snapshots cannot be changed through shared mutable references.","keywords":"History<State> dart\nclass EditorStore extends Store<EditorState> with History<EditorState> {\n  EditorStore() : super(const EditorState());\n\n  void rename(String value) {\n    commit(\n      state.copyWith(name: value),\n    );\n  }\n\n  @override\n  int get historyLimit => 100;\n}\n \n\nAPI:\n\n dart\nstore.canUndo;\nstore.canRedo;\nstore.undoCount;\nstore.redoCount;\nstore.undo();\nstore.redo();\nstore.clearHistory();\n \n\n  and   return   indicating whether a snapshot was restored.\n\nA normal "},{"title":"Streams","description":"Own external stream subscriptions inside a Store with listenTo.","url":"streams.html","text":"Streams A normal Store can own external stream subscriptions with listenTo(...). Popsicle does not require a separate Stream Store hierarchy: listenTo(...) supports: It returns the underlying StreamSubscription<T so a Store can pause, resume, or cancel early when needed. Subscriptions registered through listenTo(...) are cancelled automatically when the Store is disposed. Their lifetime therefore follows the Store, not an individual widget build.","keywords":"listenTo(...) dart\nclass LiveMessagesStore extends Store<List<Message>> {\n  LiveMessagesStore(Stream<List<Message>> messages) : super(const []) {\n    listenTo(\n      messages,\n      onData: commit,\n      onError: (error, stackTrace) {\n        effect(StreamFailed(error));\n      },\n    );\n  }\n}\n \n\nPopsicle does not require a separate Stream Store hierarchy:\n\n text\nExternal Stream\n      ↓\n   listenTo\n      ↓\n    Store\n      ↓\n   commit\n      ↓\n     .ui()\n \n\n  supports:\n\n text\nonData\nonError\nonDone\ncancelOnError\n \n\nIt returns the underlying   so a Store can pause, resume, or cancel early when needed.\n\nSubscriptions registered through "},{"title":"Async State","description":"Represent loading, data, refresh, and errors with composable AsyncState.","url":"async-state.html","text":"Async State Popsicle keeps async state as a normal immutable value: No separate async Store hierarchy is required. For multiple independent sources: or: AsyncState can preserve stale values during refresh and refresh errors, allowing UI to keep rendering useful content.","keywords":"dart\nStore<AsyncState<User>>\n \n\nNo separate async Store hierarchy is required.\n\n dart\nclass UserStore extends Store<AsyncState<User>> {\n  UserStore() : super(const AsyncState.idle());\n\n  Future<void> load() async {\n    final previous = state;\n    commit(AsyncState.loading(previous: previous));\n\n    try {\n      final user = await fetchUser();\n      commit(AsyncState.data(user));\n    } catch (error, stackTrace) {\n      commit(\n        AsyncState.error(\n          error,\n          stackTrace,\n          previous: previous,\n        ),\n      );\n    }\n  }\n}\n \n\nFor multiple independent sources:\n\n dart\nfinal combined = Async.combine2(user, posts);\n \n\nor:\n\n dart\nfinal combined = user.zip(posts);\n \n\n"},{"title":"Parameterized State","description":"Create independent Store instances keyed by runtime parameters.","url":"parameterized-state.html","text":"Parameterized State Use Popsicle.params when Store identity depends on a runtime argument. Render one instance: Each argument has an independent graph identity and cached state.","keywords":"Popsicle.params dart\nfinal user = Popsicle.params(\n  (scope, int id) => UserStore(\n    id: id,\n    repository: scope.get(userRepository),\n  ),\n);\n \n\nRender one instance:\n\n dart\nuser(42).ui(\n  (context, state, store) => UserProfile(state: state),\n);\n"},{"title":"Testing","description":"Test Popsicle state with isolated containers and typed overrides.","url":"testing.html","text":"Testing Use an isolated PopsicleContainer: Store testing: Separate containers isolate scoped reactive values and Store instances. Overrides Store handles support the same pattern:","keywords":"PopsicleContainer dart\nfinal container = PopsicleContainer();\naddTearDown(container.dispose);\n \n\n dart\nfinal count = container.get(counterValue);\ncontainer.update(counterValue, (value) => value + 1);\n \n\nStore testing:\n\n dart\nfinal store = container.store(counterStore);\nstore.increment();\nexpect(container.get(counterStore), 1);\n \n\nSeparate containers isolate scoped reactive values and Store instances.\n\n## Overrides\n\n dart\nfinal api = Popsicle.inject((_) => RealApi());\n\nfinal container = PopsicleContainer(\n  overrides: [\n    api.overrideWith((_) => FakeApi()),\n  ],\n);\n \n\nStore handles support the same pattern:\n\n dart\nfinal counter = Popsicle.create((_) => CounterStore(0));\n\nfinal container = PopsicleContainer(\n  overrides: [\n    counter.overrideWith((_) => CounterStore(10)),\n  ],\n);\n"},{"title":"API Outline","description":"Compact reference for the public Popsicle 2.1 API.","url":"api.html","text":"Popsicle 2.1 Public API Outline Core model Declarations Scope State primitives Store output Store streams Subscriptions registered through listenTo are owned by the Store and cancelled on Store disposal. History History records committed State only. Effects are never recorded or replayed. Flutter UI Explicit widgets: Async composition Dart/testing Advanced declaration types These remain public for advanced use, while application code should normally prefer Popsicle. :","keywords":"text\nmethod / Intent / Stream -> Store -> commit(State) -> .ui()\n                                   \n\n## Declarations\n\n dart\nPopsicle.inject(...)\nPopsicle.value(...)\nPopsicle.create(...)\nPopsicle.params(...)\n \n\n## Scope\n\n dart\nscope.get(source);    // non-reactive access\nscope.use(source);    // reactive dependency\nscope.store(source);  // Store instance\nscope.select(source, selector);\nscope.set(value, next);\nscope.update(value, update);\n \n\n## State primitives\n\n text\nReactiveValue<T>\nStore<State>\nIntentStore<State, Intent>\nAsyncState<T>\n \n\n## Store output\n\n dart\ncommit(nextState); // persistent state transition\neffect(value);     // one-shot occurrence\n \n\n## Store streams\n\n dart\nlistenTo(\n  stream,\n  onData: (value) => commit(...),\n  onError: (error, stackTrace) => effect(...),\n);\n \n\nSubscriptions registered through   are owned by the Store and cancelled on Store disposal.\n\n## History\n\n dart\nclass EditorStore extends Store<EditorState> with History<EditorState> {\n  // ...\n}\n \n\n dart\nstore.canUndo;\nstore.canRedo;\nstore.undoCount;\nstore.redoCount;\nstore.undo();\nstore.redo();\nstore.clearHistory();\n \n\nHistory records committed State only. Effects are never recorded or replayed.\n\n## Flutter UI\n\n dart\nreactiveValue.ui((state) => UI);\n\nstoreSource.ui(\n  (context, state, store) => UI,\n  effect: (context, effect) {},\n);\n \n\nExplicit widgets:\n\n text\nReactiveBuilder\nPopsicleConsumer\nPopsicleWidget\nPopsicleBuilder\n \n\n## Async composition\n\n dart\nAsync.combine2(a, b)\nAsync.combine3(a, b, c)\nAsync.combine4(a, b, c, d)\na.zip(b)\n \n\n## Dart/testing\n\n dart\nfinal container = PopsicleContainer();\n\ncontainer.get(source);\ncontainer.store(storeSource);\ncontainer.set(value, next);\ncontainer.update(value, update);\ncontainer.subscribe(source, listener);\ncontainer.dispose();\n \n\n## Advanced declaration types\n\nThese remain public for advanced use, while application code should normally prefer  :\n\n text\nDependency<T>\nDependency.params\nStoreHandle<Store, State>\nStoreParams<Store, State, Arg>\nPopsicleOverride\nPopsicleSubscription\nPopsicleSource\n"},{"title":"Migration to 2.1","description":"Migrate emit to commit, view to ui, and adopt History and streams.","url":"migration.html","text":"Migrating to Popsicle 2.1 Popsicle 2.1 keeps the 2.0 declaration and Scope model, while tightening the state transition vocabulary and adding opt in Store capabilities. State transition rename Use commit(...) for persistent Store state: effect(...) is unchanged and remains the one shot output channel. UI projection rename Use .ui(): The corresponding public extension names are now PopsicleStoreUi and ReactiveValueUi. Undo / redo Add History<State only to Stores that need it: Then use: Normal Stores have no history behavior or history storage. Streams A Store can now own stream subscriptions through listenTo(...): Subscriptions registered with listenTo are cancelled automatically when the Store is disposed. What did not change The recommended declarations remain: Scope remains: And the core types remain Store, IntentStore, ReactiveValue, and AsyncState.","keywords":"Scope commit(...) dart\n// Before\nemit(nextState);\n\n// 2.1\ncommit(nextState);\n \n\n  is unchanged and remains the one-shot output channel.\n\n## UI projection rename\n\nUse  :\n\n dart\n// Before\ncounter.view(\n  (context, state, store) => Text('$state'),\n);\n\n// 2.1\ncounter.ui(\n  (context, state, store) => Text('$state'),\n);\n \n\nThe corresponding public extension names are now   and  .\n\n## Undo / redo\n\nAdd   only to Stores that need it:\n\n dart\nclass EditorStore extends Store<EditorState> with History<EditorState> {\n  EditorStore() : super(const EditorState());\n}\n \n\nThen use:\n\n dart\nstore.undo();\nstore.redo();\nstore.canUndo;\nstore.canRedo;\n \n\nNormal Stores have no history behavior or history storage.\n\n## Streams\n\nA Store can now own stream subscriptions through  :\n\n dart\nclass LiveStore extends Store<LiveState> {\n  LiveStore(Stream<Event> events) : super(const LiveState()) {\n    listenTo(\n      events,\n      onData: (event) {\n        commit(state.apply(event));\n      },\n    );\n  }\n}\n \n\nSubscriptions registered with   are cancelled automatically when the Store is disposed.\n\n## What did not change\n\nThe recommended declarations remain:\n\n dart\nPopsicle.inject(...)\nPopsicle.value(...)\nPopsicle.create(...)\nPopsicle.params(...)\n \n\n  remains:\n\n dart\nscope.get(...)\nscope.use(...)\nscope.store(...)\nscope.select(...)\n \n\nAnd the core types remain  ,  ,  , and "},{"title":"Changelog","description":"Release history for Popsicle.","url":"changelog.html","text":"2.1.0 Renamed Store persistent state transitions from emit(...) to commit(...). Standardized the high level Flutter projection API on .ui() for Stores and ReactiveValue. Added opt in History<State with: undo() / redo() canUndo / canRedo undoCount / redoCount clearHistory() configurable historyLimit History records committed state snapshots only; one shot effects are never recorded or replayed. Added Store listenTo(...) for Store owned stream subscriptions. Stream subscriptions registered through listenTo(...) are cancelled automatically when the Store is disposed. listenTo(...) supports onData, onError, onDone, and cancelOnError, and returns the underlying subscription for explicit pause/resume/early cancellation. Renamed the public UI extension types to PopsicleStoreUi and ReactiveValueUi. Added History and Stream examples and tests. Updated README, API outline, migration notes, publishing guide, example documentation, and wiki for the 2.1 API. 2.0.0 Added the unified declaration namespace: Popsicle.inject(...) Popsicle.value(...) Popsicle.create(...) Popsicle.params(...) Added public Scope with scope.get(...) and scope.use(...) semantics. Replaced public Ref/read/watch usage in Popsicle APIs and examples with Scope. Added scope.store(...) and scope.select(...) Store helpers. Added scope.set(...) and scope.update(...) ReactiveValue helpers. Added the compact Flutter projection API for both ReactiveValue and Store handles. Extended Store UI projection to parameterized Store handles returned by Popsicle.params. Updated PopsicleWidget and PopsicleBuilder to expose Scope. Wrapped the Dart only graph in PopsicleContainer with get, subscribe, and scoped Store/value helpers. Kept Store<State and IntentStore<State, Intent as the primary structured state types. Renamed the internal engine barrel to engine.dart; upstream attribution remains in NOTICE and LICENSE. 0.1.4 Added ReactiveValue<T for small standalone mutable reactive state. Added scoped set and update helpers. 0.1.3 Simplified PopsicleConsumer to Store state + dedicated one shot effects. Added the Store effect channel. 0.1.2 Added the Store focused consumer and expanded examples. 0.1.1 Fixed Flutter consumer integration after vendoring the engine. 0.1.0 Initial Popsicle API iteration on the vendored 2.6.1 engine baseline.","keywords":"emit(...) commit(...) .ui() ReactiveValue History<State> undo() redo() canUndo canRedo undoCount redoCount clearHistory() historyLimit listenTo(...) listenTo(...) listenTo(...) onData onError onDone cancelOnError PopsicleStoreUi ReactiveValueUi Popsicle.inject(...) Popsicle.value(...) Popsicle.create(...) Popsicle.params(...) Scope scope.get(...) scope.use(...) Ref read watch Scope scope.store(...) scope.select(...) scope.set(...) scope.update(...) ReactiveValue Popsicle.params PopsicleWidget PopsicleBuilder Scope PopsicleContainer get subscribe Store<State> IntentStore<State, Intent> engine.dart NOTICE LICENSE ReactiveValue<T> set update PopsicleConsumer"},{"title":"Publishing","description":"Validate, tag, publish, and document a Popsicle release.","url":"publishing.html","text":"Publishing Popsicle Current release: 2.1.0 1. Validate locally Run from the package root: Run the example app as an integration check: The 2.1 example includes dependency injection, ReactiveValue, Store .ui(), IntentStore, History, streams, async state, combined async sources, and parameterized Stores. 2. Verify release metadata Confirm: pubspec.yaml says 2.1.0 CHANGELOG.md starts with 2.1.0 README examples use commit(...) and .ui() History<State is exported stream examples use Store listenTo(...) LICENSE and NOTICE are included repository/homepage/issue tracker/documentation URLs are correct screenshots referenced by pubspec.yaml exist generated build output, .dart tool, and repository local .git data are not included in the publish archive 3. Tag the release Use the repository's actual default branch if it is not main. 4. Publish Review the package contents before confirming. 5. Wiki The wiki/ directory contains the Markdown pages for the 2.1 API. Push/copy those pages to the GitHub Wiki repository when publishing documentation updates.","keywords":"bash\nflutter pub get\nflutter analyze\nflutter test\nflutter pub publish --dry-run\n \n\nRun the example app as an integration check:\n\n bash\ncd example\nflutter pub get\nflutter run\n \n\nThe 2.1 example includes dependency injection, ReactiveValue, Store  , IntentStore, History, streams, async state, combined async sources, and parameterized Stores.\n\n## 2. Verify release metadata\n\nConfirm:\n\n-   says  \n-   starts with  \n- README examples use   and  \n-   is exported\n- stream examples use Store  \n-   and   are included\n- repository/homepage/issue tracker/documentation URLs are correct\n- screenshots referenced by   exist\n- generated build output,  , and repository-local   data are not included in the publish archive\n\n## 3. Tag the release\n\n bash\ngit add .\ngit commit -m \"release: popsicle 2.1.0\"\ngit tag v2.1.0\ngit push origin main --tags\n \n\nUse the repository's actual default branch if it is not  .\n\n## 4. Publish\n\n bash\nflutter pub publish\n \n\nReview the package contents before confirming.\n\n## 5. Wiki\n\nThe "}]